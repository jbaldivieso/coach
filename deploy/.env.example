# Coach Production Environment
# Copy this to .env and customize
#
# Generate a secret key with:
#   python3 -c 'import secrets; print(secrets.token_urlsafe(50))'

DJANGO_SETTINGS_MODULE=config.settings
DJANGO_DEBUG=False
DJANGO_SECRET_KEY=generate-a-secure-key-here

# ============================================================
# OPTION A: Local Network / Raspberry Pi (HTTP)
# ============================================================
# DJANGO_ALLOWED_HOSTS=basement.local,192.168.1.xxx
# CORS_ALLOWED_ORIGINS=http://basement.local:8000,http://192.168.1.xxx:8000
# CSRF_TRUSTED_ORIGINS=http://basement.local:8000,http://192.168.1.xxx:8000

# ============================================================
# OPTION B: Cloud Server with Domain + SSL (HTTPS)
# ============================================================
DJANGO_ALLOWED_HOSTS=coach.baldivieso.com
CORS_ALLOWED_ORIGINS=https://coach.baldivieso.com
CSRF_TRUSTED_ORIGINS=https://coach.baldivieso.com
